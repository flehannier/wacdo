spring:
  application:
    name: wacdo
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/wacdo?currentSchema=wacdo&zeroDateTimeBehavior=CONVERT_TO_NULL&serverTimezone=UTC
    username: root
    password: wacdo@2025$
    hikari:
      connection-test-query: SELECT 1
      test-while-idle: true
  security:
    enabled: true
  jpa:
    show_sql: true
    hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect
      ddl-auto: update
